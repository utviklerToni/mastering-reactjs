import React, { Fragment } from 'react';
import SearchBar from '../Practice_section/components/SearchComponent/SearchBar';

const ProjectLists = () => {
	return (
		<Fragment>
			<SearchBar />
		</Fragment>
	);
};

export default ProjectLists;
